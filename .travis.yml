language: c
sudo: required
dist: trusty

before_install:
  - sudo apt-get install gfortran
  - sudo apt-get install libnetcdf-dev libgd2-xpm-dev build-essential libnetcdf6 libgd2-xpm netcdf-doc netcdf-bin

script:
  - ./build_glm.sh
  - ./glm-aed/src/glm --help

after_success:
